# L00 环境自检 — 证据

> 执行日期：2026-09-25 · 执行者：Claude（GLM）· 口径：macOS + Claude Code（ADR-0002/0004）
> 讲义：`docs/lessons/L00-环境自检.md` · 全程在 master 执行（ADR-0004 例外条款，无代码增量）

## 环境前置

- `vendors/CodexFDE` 与 `vendors/flowERP` 起始 `git status --porcelain` 均为空，退出码 0（基线干净）。
- 两个 `.venv` 均从零创建（此前不存在）。
- 本机 Python：系统 3.13.5（课堂口径为 3.11；pyproject 要求 ≥3.10，偏离见文末说明）。

## C1 — CodexFDE `.venv` 建立与可编辑安装

```bash
cd vendors/CodexFDE
python3 -m venv .venv          # 输出：无（静默成功）
.venv/bin/python --version     # Python 3.13.5
.venv/bin/pip install -e .     # Successfully installed flowerp-fde-camp-0.1.0
```

- `python3 -m venv .venv` 退出码 **0**
- `pip install -e .` 退出码 **0**，末行 `Successfully installed flowerp-fde-camp-0.1.0`（构建 editable wheel 成功）

**结论：通过（blocking）**

## C2 — 工作台 blocking eval

讲义命令 `.venv/bin/flowerp-workbench eval.harness --suite blocking` 实测无效：`flowerp-workbench --help` 的子命令列表中**不存在** `eval.harness`（argparse 报 `invalid choice`，退出码 2）。按讲义"以 `--help` 实际输出为准"规则，改用等价入口 `.venv/bin/python -m eval.harness --suite blocking`（模块 `eval/harness.py` 自带 `--suite {all,blocking,observing}` 参数，`--help` 核对通过）。

```bash
.venv/bin/python -m eval.harness --suite blocking
```

退出码 **0**。末行统计：

```json
{"total": 10, "passed": 10, "blocking_failed": 0, "observing_failed": 0, "decision": "pass"}
```

10 项逐条 `[PASS]`：spec_contract_rejects_ambiguity、isolated_report_contract_is_honest、bootstrap_evidence_is_honest、plugin_lifecycle_is_reversible、delivery_evidence_and_review_controls、web_api_and_persistence_projection_agree、ci_evidence_envelope_is_honest、write_sets_reject_conflict、raw_feedback_cannot_become_blocking、no_committed_secrets。

**结论：通过（blocking），10/10 与讲义预期一致**

## C3 — flowERP `.venv` 建立与 blocking eval

```bash
cd ../flowERP
python3 -m venv .venv                              # 退出码 0
.venv/bin/python --version                         # Python 3.13.5
.venv/bin/pip install -e .                         # Successfully installed flowerp-0.1.0，退出码 0
.venv/bin/python -X utf8 -m eval.harness --help    # usage 核对一致（--suite {all,blocking,observing}），退出码 0
.venv/bin/python -X utf8 -m eval.harness --suite blocking
```

退出码 **0**。末行统计：

```json
{"total": 19, "passed": 19, "blocking_failed": 0, "observing_failed": 0, "decision": "pass"}
```

19 项逐条 `[PASS]`（摘要）：inventory_export_is_stable、stock_never_negative、receiving_is_idempotent、cancellation_releases_reservation、illegal_transition_is_blocked、purchase_requires_approval、purchase_request_preserves_reason、order_total_matches_lines、ecommerce_channel_order_is_idempotent_and_guarded、channel_callback_lease_is_exclusive_and_bounded、production_schema_invariants、multi_location_transfer_conserves_stock、stale_stock_count_is_blocked、sales_credit_and_atomic_reservation、backup_is_restorable、purchase_invoice_three_way_match、payable_aging_tracks_open_supplier_exposure、double_entry_fifo_and_subledger_reconciliation、bank_statement_control_and_reconciliation。

**结论：通过（blocking），19/19 与讲义预期一致**

## C4 — 工作台环境自检

```bash
cd ../CodexFDE
.venv/bin/flowerp-workbench environment-check
```

退出码 **0**。命令本体输出：`"ok": true`，`"scope": "installation"`，`"product_checked": false`。6 项检查全 `ok: true`，其中 `virtual_environment` 指向 `vendors/CodexFDE/.venv/bin/python3`（证明用的是仓库 `.venv`，非系统/conda Python）。

**结论：通过（blocking）**

## C5 — 客户环境自检

```bash
.venv/bin/flowerp-workbench environment-check --product --product-root ../flowERP
```

退出码 **0**。命令本体输出：`"ok": true`，`"product_checked": true`。基础 6 项 + `flowerp` 项全过；`flowerp` 检查明示 python 解析到 `vendors/flowERP/.venv/bin/python`、包定位到 flowERP 仓库内 `flowerp/__init__.py` 与 `eval/__init__.py`——两仓库不共享进程边界的实操体现。

**结论：通过（blocking）**

## C6 — course-status 现状记录

```bash
.venv/bin/flowerp-workbench course-status
```

退出码 **0**。命令本体输出关键项：`"contract_valid": true`，`"course_ready": false`，`lesson_count: 16`，`errors: []`，warning："缺少 16 个逐讲起始基线；当前终态代码不能替代渐进式学习证据"，并列出 `course/l01-start` … `course/l16-start` 共 16 个 `missing_baseline_refs`。

**解释**：`course_ready: false` 属预期——参考仓库未打逐讲基线标签，该门已被 ADR-0001 放弃，不构成失败。此处如实记录，不掩盖为"通过"。**结论：通过（observing）**

## C7 — 版本台账

| 工具 | 版本 | 备注 |
|---|---|---|
| python3 | Python 3.13.5 | 课堂口径 3.11，偏离见下 |
| git | git version 2.24.3 (Apple Git-128) | — |
| claude | 2.1.282 (Claude Code) | 本复刻口径的代理 CLI（ADR-0002） |
| codex | codex-cli 0.147.0 | 原课程口径，本机已装、记录备查 |

四条命令整体退出码 **0**。

## 复刻产物零写回核验

- `git -C vendors/CodexFDE status --porcelain` → 空，退出码 0
- `git -C vendors/flowERP status --porcelain` → 空，退出码 0
- `.venv` 在两仓库均被 `.gitignore` 覆盖（`git check-ignore .venv` 确认），运行产物不入库

## 偏离与说明

1. **命令形式偏离**：讲义 C2 写作 `flowerp-workbench eval.harness --suite blocking`，实际 CLI 无该子命令（退出码 2 佐证）。按讲义预案以 `--help` 为准，等价命令为 `.venv/bin/python -m eval.harness --suite blocking`，参数 `--suite blocking` 与讲义一致。
2. **Python 版本偏离**：课堂统一 3.11，本机系统 Python 3.13.5 建 `.venv`。pyproject 只要求 ≥3.10，两仓库安装与全部 eval 在 3.13 下通过，偏离记录于此，不构成失败。
3. 其余命令（venv、pip install -e、flowERP eval、environment-check ×2、course-status）与讲义逐字一致。

## C1-C7 逐项结论

| # | 验收项 | 级别 | 结果 | 依据 |
|---|---|---|---|---|
| C1 | CodexFDE `.venv` + 可编辑安装 | blocking | ✅ 通过 | `pip install -e .` 退出 0，`flowerp-fde-camp-0.1.0` |
| C2 | 工作台 blocking eval | blocking | ✅ 通过 | 10/10，`decision: pass`，退出 0 |
| C3 | flowERP `.venv` + blocking eval | blocking | ✅ 通过 | 安装退出 0；19/19，退出 0 |
| C4 | 工作台 environment-check | blocking | ✅ 通过 | 本体输出 `ok: true`，virtual_environment 指向仓库 `.venv` |
| C5 | 客户 environment-check --product | blocking | ✅ 通过 | 本体输出 `ok: true`，flowerp 解析到 flowERP `.venv` |
| C6 | course-status 现状 | observing | ✅ 记录 | `contract_valid: true`；`course_ready: false` 为 ADR-0001 预期 |
| C7 | 版本台账 | blocking | ✅ 通过 | python/git/claude/codex 四项齐（见上表） |

---

## 具名验收

- 验收人：XianReallyHot-ZZH（口头确认"没问题"，委托 Claude 代填此行）
- 日期：2026-09-25
- 结论：接受
